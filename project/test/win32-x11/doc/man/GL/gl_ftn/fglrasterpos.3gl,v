head	1.2;
access;
symbols
	win32-x11-2001-03-26:1.1.1.1 SNAPSHOT:1.1.1;
locks; strict;
comment	@# @;
expand	@o@;


1.2
date	2001.06.03.20.47.17;	author ssiddiqi;	state Exp;
branches;
next	1.1;

1.1
date	2001.03.25.19.36.54;	author ssiddiqi;	state Exp;
branches
	1.1.1.1;
next	;

1.1.1.1
date	2001.03.25.19.36.54;	author ssiddiqi;	state Exp;
branches;
next	;


desc
@@


1.2
log
@Win32-X11 update sources check in, June 3, 2001 - ssiddiqi
@
text
@'\"! eqn | mmdoc
'\"macro stdmacro
.ds Vn Version 1.2
.ds Dt 10 Nov 1998
.ds Re Release 1.0
.ds Dp Apr 04 17:40
.ds Dm  Apr  4  200
.ds Xs 7689 14 fglrasterpos.gl
.TH FGLRASTERPOS 
.SH NAME
.B "fglRasterPos2d, fglRasterPos2f, fglRasterPos2i, fglRasterPos2s, fglRasterPos3d, fglRasterPos3f, fglRasterPos3i, fglRasterPos3s, fglRasterPos4d, fglRasterPos4f, fglRasterPos4i, fglRasterPos4s, fglRasterPos2dv, fglRasterPos2fv, fglRasterPos2iv, fglRasterPos2sv, fglRasterPos3dv, fglRasterPos3fv, fglRasterPos3iv, fglRasterPos3sv, fglRasterPos4dv, fglRasterPos4fv, fglRasterPos4iv, fglRasterPos4sv
\- specify the raster position for pixel operations

.SH FORTRAN SPECIFICATION
SUBROUTINE \f3fglRasterPos2d\fP(
REAL*8 \fIx\fP,
.nf
.ta \w'\f3SUBROUTINE \fPfglRasterPos2d( 'u
	REAL*8 \fIy\fP )
.fi
SUBROUTINE \f3fglRasterPos2f\fP(
REAL*4 \fIx\fP,
.nf
.ta \w'\f3SUBROUTINE \fPfglRasterPos2f( 'u
	REAL*4 \fIy\fP )
.fi
SUBROUTINE \f3fglRasterPos2i\fP(
INTEGER*4 \fIx\fP,
.nf
.ta \w'\f3SUBROUTINE \fPfglRasterPos2i( 'u
	INTEGER*4 \fIy\fP )
.fi
SUBROUTINE \f3fglRasterPos2s\fP(
INTEGER*2 \fIx\fP,
.nf
.ta \w'\f3SUBROUTINE \fPfglRasterPos2s( 'u
	INTEGER*2 \fIy\fP )
.fi
SUBROUTINE \f3fglRasterPos3d\fP(
REAL*8 \fIx\fP,
.nf
.ta \w'\f3SUBROUTINE \fPfglRasterPos3d( 'u
	REAL*8 \fIy\fP,
	REAL*8 \fIz\fP )
.fi
SUBROUTINE \f3fglRasterPos3f\fP(
REAL*4 \fIx\fP,
.nf
.ta \w'\f3SUBROUTINE \fPfglRasterPos3f( 'u
	REAL*4 \fIy\fP,
	REAL*4 \fIz\fP )
.fi
SUBROUTINE \f3fglRasterPos3i\fP(
INTEGER*4 \fIx\fP,
.nf
.ta \w'\f3SUBROUTINE \fPfglRasterPos3i( 'u
	INTEGER*4 \fIy\fP,
	INTEGER*4 \fIz\fP )
.fi
SUBROUTINE \f3fglRasterPos3s\fP(
INTEGER*2 \fIx\fP,
.nf
.ta \w'\f3SUBROUTINE \fPfglRasterPos3s( 'u
	INTEGER*2 \fIy\fP,
	INTEGER*2 \fIz\fP )
.fi
SUBROUTINE \f3fglRasterPos4d\fP(
REAL*8 \fIx\fP,
.nf
.ta \w'\f3SUBROUTINE \fPfglRasterPos4d( 'u
	REAL*8 \fIy\fP,
	REAL*8 \fIz\fP,
	REAL*8 \fIw\fP )
.fi
SUBROUTINE \f3fglRasterPos4f\fP(
REAL*4 \fIx\fP,
.nf
.ta \w'\f3SUBROUTINE \fPfglRasterPos4f( 'u
	REAL*4 \fIy\fP,
	REAL*4 \fIz\fP,
	REAL*4 \fIw\fP )
.fi
SUBROUTINE \f3fglRasterPos4i\fP(
INTEGER*4 \fIx\fP,
.nf
.ta \w'\f3SUBROUTINE \fPfglRasterPos4i( 'u
	INTEGER*4 \fIy\fP,
	INTEGER*4 \fIz\fP,
	INTEGER*4 \fIw\fP )
.fi
SUBROUTINE \f3fglRasterPos4s\fP(
INTEGER*2 \fIx\fP,
.nf
.ta \w'\f3SUBROUTINE \fPfglRasterPos4s( 'u
	INTEGER*2 \fIy\fP,
	INTEGER*2 \fIz\fP,
	INTEGER*2 \fIw\fP )
.fi

.EQ
delim $$
.EN
.SH PARAMETERS
.TP \w'\f2x\fP\ \f2y\fP\ \f2z\fP\ \f2w\fP\ \ 'u 
\f2x\fP, \f2y\fP, \f2z\fP, \f2w\fP
Specify the $x$, $y$, $z$, and $w$ object coordinates
(if present)
for the raster position.
.SH FORTRAN SPECIFICATION
SUBROUTINE \f3fglRasterPos2dv\fP(
CHARACTER*8 \fIv\fP )
.nf
.fi
SUBROUTINE \f3fglRasterPos2fv\fP(
CHARACTER*8 \fIv\fP )
.nf
.fi
SUBROUTINE \f3fglRasterPos2iv\fP(
CHARACTER*8 \fIv\fP )
.nf
.fi
SUBROUTINE \f3fglRasterPos2sv\fP(
CHARACTER*8 \fIv\fP )
.nf
.fi
SUBROUTINE \f3fglRasterPos3dv\fP(
CHARACTER*8 \fIv\fP )
.nf
.fi
SUBROUTINE \f3fglRasterPos3fv\fP(
CHARACTER*8 \fIv\fP )
.nf
.fi
SUBROUTINE \f3fglRasterPos3iv\fP(
CHARACTER*8 \fIv\fP )
.nf
.fi
SUBROUTINE \f3fglRasterPos3sv\fP(
CHARACTER*8 \fIv\fP )
.nf
.fi
SUBROUTINE \f3fglRasterPos4dv\fP(
CHARACTER*8 \fIv\fP )
.nf
.fi
SUBROUTINE \f3fglRasterPos4fv\fP(
CHARACTER*8 \fIv\fP )
.nf
.fi
SUBROUTINE \f3fglRasterPos4iv\fP(
CHARACTER*8 \fIv\fP )
.nf
.fi
SUBROUTINE \f3fglRasterPos4sv\fP(
CHARACTER*8 \fIv\fP )
.nf
.fi

.SH PARAMETERS
.TP
\f2v\fP
Specifies a pointer to an array of two,
three,
or four elements,
specifying $x$, $y$, $z$, and $w$ coordinates, respectively.
.SH DESCRIPTION
The GL maintains a 3D position in window coordinates.
This position,
called the raster position,
is used to position pixel and bitmap write operations. It is
maintained with subpixel accuracy.
See \f3fglBitmap\fP, \f3fglDrawPixels\fP, and \f3fglCopyPixels\fP.
.P
The current raster position consists of three window coordinates
($x$, $y$, $z$),
a clip coordinate value ($w$),
an eye coordinate distance,
a valid bit,
and associated color data and texture coordinates.
The $w$ coordinate is a clip coordinate,
because $w$ is not projected to window coordinates.
\f3fglRasterPos4\fP specifies object coordinates $x$, $y$, $z$, and $w$
explicitly.
\f3fglRasterPos3\fP specifies object coordinate $x$, $y$, and $z$ explicitly,
while $w$ is implicitly set to 1.
\f3fglRasterPos2\fP uses the argument values for $x$ and $y$ while
implicitly setting $z$ and $w$ to 0 and 1.  
.P
The object coordinates presented by \f3fglRasterPos\fP are treated just like those
of a \f3fglVertex\fP command:
They are transformed by the current modelview and projection matrices
and passed to the clipping stage.
If the vertex is not culled,
then it is projected and scaled to window coordinates,
which become the new current raster position,
and the \f3GL_CURRENT_RASTER_POSITION_VALID\fP flag is set.
If the vertex 
.I is
culled,
then the valid bit is cleared and the current raster position
and associated color and texture coordinates are undefined.
.P
The current raster position also includes some associated color data
and texture coordinates.
If lighting is enabled,
then \f3GL_CURRENT_RASTER_COLOR\fP
(in RGBA mode)
or \f3GL_CURRENT_RASTER_INDEX\fP
(in color index mode)
is set to the color produced by the lighting calculation
(see \f3fglLight\fP, \f3fglLightModel\fP, and 
.br
\f3fglShadeModel\fP).
If lighting is disabled, 
current color
(in RGBA mode, state variable \f3GL_CURRENT_COLOR\fP)
or color index
(in color index mode, state variable \f3GL_CURRENT_INDEX\fP)
is used to update the current raster color.
.P
Likewise,
\f3GL_CURRENT_RASTER_TEXTURE_COORDS\fP is updated as a function
of \f3GL_CURRENT_TEXTURE_COORDS\fP,
based on the texture matrix and the texture generation functions
(see \f3fglTexGen\fP).
Finally,
the distance from the origin of the eye coordinate system to the
vertex as transformed by only the modelview matrix replaces
\f3GL_CURRENT_RASTER_DISTANCE\fP.
.P
Initially, the current raster position is (0, 0, 0, 1),
the current raster distance is 0,
the valid bit is set,
the associated RGBA color is (1, 1, 1, 1),
the associated color index is 1,
and the associated texture coordinates are (0, 0, 0, 1).
In RGBA mode,
\f3GL_CURRENT_RASTER_INDEX\fP is always 1;
in color index mode,
the current raster RGBA color always maintains its initial value.
.SH NOTES
The raster position is modified both by \f3fglRasterPos\fP and by \f3fglBitmap\fP. 
.P
When the raster position coordinates are invalid,
drawing commands that are based on the raster position are
ignored (that is, they do not result in changes to GL state).
.P
Calling \f3fglDrawElements\fP, or \f3fglDrawRangeElements\fP may leave the
current color or index indeterminate.
If \f3fglRasterPos\fP is executed while the current color or index is indeterminate, the
current raster color or current raster index remains indeterminate.
.P
To set a valid raster position outside the viewport, first set a valid
raster position, then call \f3fglBitmap\fP with NULL as the \f2bitmap\fP
parameter.
.P
When the _arbstring(imaging) extension is supported, there are distinct
raster texture coordinates for each texture unit. Each texture unit's
current ratster texture coordinates are updated by \f3fglRasterPos\fP.
.SH ERRORS
\f3GL_INVALID_OPERATION\fP is generated if \f3fglRasterPos\fP
is executed between the execution of \f3fglBegin\fP
and the corresponding execution of \f3fglEnd\fP.
.SH ASSOCIATED GETS
\f3fglGet\fP with argument \f3GL_CURRENT_RASTER_POSITION\fP
.br
\f3fglGet\fP with argument \f3GL_CURRENT_RASTER_POSITION_VALID\fP
.br
\f3fglGet\fP with argument \f3GL_CURRENT_RASTER_DISTANCE\fP
.br
\f3fglGet\fP with argument \f3GL_CURRENT_RASTER_COLOR\fP
.br
\f3fglGet\fP with argument \f3GL_CURRENT_RASTER_INDEX\fP
.br
\f3fglGet\fP with argument \f3GL_CURRENT_RASTER_TEXTURE_COORDS\fP
.SH SEE ALSO
\f3fglBitmap\fP,
\f3fglCopyPixels\fP,
\f3fglDrawArrays\fP,
\f3fglDrawElements\fP,
\f3fglDrawRangeElements\fP,
\f3fglDrawPixels\fP,
\f3fglTexCoord\fP,
\f3fglTexGen\fP,
\f3fglVertex\fP
@


1.1
log
@Initial revision
@
text
@d1 2
d6 94
a99 5
.ds Dp Feb 04 11:05
.ds Dm 4 Feb  4 11:
.ds Xs 20383     7
_header(RasterPos,specify the raster position for pixel operations)
_names(RasterPos,[2-4][sifd])
d104 2
a105 1
_phead(_param1 _param2 _param3 _param4)
d109 50
a158 1
_names(RasterPos,[2-4][sifd]v)
d160 2
a161 1
_phead(_param1)
d172 1
a172 1
See _cmnd(Bitmap), _cmnd(DrawPixels), and _cmnd(CopyPixels).
d182 1
a182 1
_cmnd(RasterPos4) specifies object coordinates $x$, $y$, $z$, and $w$
d184 1
a184 1
_cmnd(RasterPos3) specifies object coordinate $x$, $y$, and $z$ explicitly,
d186 1
a186 1
_cmnd(RasterPos2) uses the argument values for $x$ and $y$ while
d189 2
a190 2
The object coordinates presented by _cmnd are treated just like those
of a _cmnd(Vertex) command:
d211 1
a211 1
(see _cmnd(Light), _cmnd(LightModel), and 
d213 1
a213 1
_cmnd(ShadeModel)).
d225 1
a225 1
(see _cmnd(TexGen)).
d242 1
a242 1
The raster position is modified both by _cmnd and by _cmnd(Bitmap). 
d248 1
a248 1
Calling _cmnd(DrawElements), or _cmnd(DrawRangeElements) may leave the
d250 1
a250 1
If _cmnd is executed while the current color or index is indeterminate, the
d254 1
a254 1
raster position, then call _cmnd(Bitmap) with NULL as the \f2bitmap\fP
d259 1
a259 1
current ratster texture coordinates are updated by _cmnd.
d261 3
a263 3
\f3GL_INVALID_OPERATION\fP is generated if _cmnd
is executed between the execution of _cmnd(Begin)
and the corresponding execution of _cmnd(End).
d265 1
a265 1
_cmnd(Get) with argument \f3GL_CURRENT_RASTER_POSITION\fP
d267 1
a267 1
_cmnd(Get) with argument \f3GL_CURRENT_RASTER_POSITION_VALID\fP
d269 1
a269 1
_cmnd(Get) with argument \f3GL_CURRENT_RASTER_DISTANCE\fP
d271 1
a271 1
_cmnd(Get) with argument \f3GL_CURRENT_RASTER_COLOR\fP
d273 1
a273 1
_cmnd(Get) with argument \f3GL_CURRENT_RASTER_INDEX\fP
d275 1
a275 1
_cmnd(Get) with argument \f3GL_CURRENT_RASTER_TEXTURE_COORDS\fP
d277 9
a285 9
_cmnd(Bitmap),
_cmnd(CopyPixels),
_cmnd(DrawArrays),
_cmnd(DrawElements),
_cmnd(DrawRangeElements),
_cmnd(DrawPixels),
_cmnd(TexCoord),
_cmnd(TexGen),
_cmnd(Vertex)
@


1.1.1.1
log
@win32-x11 source tree update March 26, 2001
@
text
@@
